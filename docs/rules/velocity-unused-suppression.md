# `velocity/unused-suppression`

Reports a valid directive that matched no finding. Default: **info**.

Stale suppressions obscure intent and could hide a future finding on the same line. Remove the directive or move it immediately above the reviewed operation.
